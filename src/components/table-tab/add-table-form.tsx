import { Sheet } from "@/components/components/sheet";

export const AddTableForm = () => {
	return (
		<Sheet title="Add Table" name="add-table">
			<h1>Add a new table to the database</h1>
		</Sheet>
	);
};
